const $admin_product_wapper = document.querySelector('.admin_product_wapper');

const getProduct = async () => {};
$admin_product_wapper.addEventListener('click', () => {});
$admin_product_wapper.addEventListener('change', () => {});
$admin_product_wapper.addEventListener('submit', () => {});
